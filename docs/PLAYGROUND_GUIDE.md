# Prompt Playground - Руководство пользователя

## Обзор

Prompt Playground — интерактивная среда для отладки и тестирования промптов Gemini с real-time логами и визуальной обратной связью.

## Ключевые возможности

### 1. **Prompt Builder** (Конструктор промптов)

- ✅ **Секции промпта** с toggle (вкл/выкл)
- ✅ **Drag & Drop** для изменения порядка секций
- ✅ **Live редактирование** текста каждой секции
- ✅ **Переменные** (например, `{{subject_mode}}`, `{{framing_hint}}`)

**Как использовать:**
1. Нажмите на иконку глаза (👁️), чтобы включить/выключить секцию
2. Используйте стрелки (↑↓) для изменения порядка секций
3. Редактируйте текст секций напрямую в textarea
4. Изменяйте переменные в разделе "Configuration"

### 2. **JSON Preview** (Предпросмотр запроса)

- ✅ Показывает **точный JSON**, который отправляется в Gemini API
- ✅ Структура запроса: `{ model, generationConfig, contents[0].parts = [image1, image2, text] }`
- ✅ Обновляется в реальном времени при изменении промпта

**Что видно:**
```json
{
  "model": "gemini-2.0-flash-exp",
  "generationConfig": {
    "temperature": 0.7,
    "responseMimeType": "image/png"
  },
  "contents": [
    {
      "role": "user",
      "parts": [
        { "inline_data": { "mime_type": "image/jpeg", "data": "<IMAGE_1_BASE64>" } },
        { "inline_data": { "mime_type": "image/png", "data": "<IMAGE_2_BASE64>" } },
        { "text": "[CRITICAL]\n...\n\n[SCENE]\n..." }
      ]
    }
  ]
}
```

### 3. **Test Panel** (Панель тестирования)

- ✅ Загрузка 2 изображений (Subject + Style Reference)
- ✅ Кнопка "Test Prompt" для запуска генерации
- ✅ Показывает прогресс генерации

**Workflow:**
1. Загрузите Image 1 (фото пользователя - "Subject")
2. Загрузите Image 2 (референс стиля - "Style Reference") - опционально
3. Нажмите "Test Prompt"
4. Дождитесь результата (обычно 8-15 секунд)

### 4. **Real-time Logs** (Логи в реальном времени)

- ✅ **SSE stream** - логи приходят мгновенно
- ✅ Цветная подсветка по уровням (INFO, WARNING, ERROR)
- ✅ Детальная информация в `extra` полях

**Примеры логов:**
```
[INFO] 2026-02-04T15:23:09Z test_started
  { "model": "gemini-2.0-flash-exp", "sections": 6 }

[INFO] 2026-02-04T15:23:09Z image_uploaded
  { "index": 1, "filename": "user_photo.jpg", "size_bytes": 152340 }

[INFO] 2026-02-04T15:23:10Z request_prepared
  { "parts_count": 3, "images": 2, "request_structure": "parts = [image, image, text]" }

[INFO] 2026-02-04T15:23:10Z generation_started
  { "provider": "gemini" }

[INFO] 2026-02-04T15:23:18Z generation_completed
  { "duration": 7.85, "finish_reason": "SUCCESS", "model": "gemini-2.0-flash-exp" }
```

**При ошибках:**
```
[ERROR] 2026-02-04T15:23:18Z generation_failed
  {
    "error": "ImageGenerationError: The model could not generate the image",
    "finish_reason": "IMAGE_OTHER",
    "finish_message": "The two images have incompatible aspect ratios for composition",
    "duration": 3.21
  }
```

### 5. **Result Viewer** (Просмотр результатов)

- ✅ **Сгенерированное изображение** (base64 preview)
- ✅ **Duration** - время генерации
- ✅ **Raw Gemini Response** - полный ответ API (sanitized)

**Что показывает Raw Response:**
```json
{
  "candidates": [
    {
      "content": {
        "parts": [
          {
            "inlineData": {
              "mimeType": "image/png",
              "data": "<base64 123456 bytes>"
            }
          }
        ],
        "role": "model"
      },
      "finishReason": "STOP",
      "safetyRatings": [
        { "category": "HARM_CATEGORY_HATE_SPEECH", "probability": "NEGLIGIBLE" }
      ]
    }
  ],
  "promptFeedback": {
    "safetyRatings": [...]
  }
}
```

## Типовые сценарии использования

### Сценарий 1: Отладка нового тренда

1. **Загрузить тренд в Playground**
   - Выберите тренд из dropdown "Load Trend..."
   - Playground автоматически заполнит все секции промпта из тренда
   
2. **Загрузить тестовые фото**
   - Image 1: фото пользователя
   - Image 2: референс стиля (если тренд использует style reference)
   
3. **Протестировать**
   - Нажать "Test Prompt"
   - Посмотреть логи в реальном времени
   - Проверить результат
   
4. **Итерации**
   - Изменить формулировку секции SCENE
   - Test → Result → Adjust → Test снова
   - Повторять, пока не найдете правильную формулировку
   
5. **Сохранить**
   - Скопировать финальные секции обратно в тренд (вручную пока)

### Сценарий 2: Понять, почему IMAGE_OTHER

1. **Воспроизвести ошибку**
   - Загрузите те же фото, которые вызвали ошибку
   - Нажмите "Test Prompt"
   
2. **Проверить логи**
   - Посмотрите в Real-time Logs на ERROR
   - Найдите `finish_message` - там точная причина от Gemini
   
3. **Проверить Raw Response**
   - Вкладка "Result" → Raw Gemini Response
   - Посмотрите `promptFeedback.blockReason` или `candidates[0].finishMessage`
   
4. **Исправить проблему**
   - Например, если `"incompatible aspect ratios"` → измените size/crop фото
   - Если `"face partially obscured"` → используйте фото с открытым лицом
   - Если `"safety violation"` → проверьте content фото/промпта

### Сценарий 3: Экспериментировать с порядком частей

1. **Текущий порядок**
   - JSON Preview показывает: `parts = [image1, image2, text]`
   
2. **Изменить порядок секций**
   - Используйте стрелки ↑↓ для перестановки секций SCENE, STYLE и т.д.
   - JSON Preview автоматически обновится
   
3. **Попробовать разные комбинации**
   - Test с `[image1, image2, text]`
   - Изменить на `[text, image1, image2]` (переместите SCENE в начало)
   - Test снова
   - Сравните результаты

**Важно:** Gemini рекомендует `images first, then text` для composition задач.

### Сценарий 4: A/B тестирование секций промпта

1. **Baseline**
   - Включены все секции → Test → Result A
   
2. **Test без CRITICAL**
   - Выключите секцию CRITICAL (👁️ → 👁️‍🗨️)
   - Test → Result B
   
3. **Сравнить A и B**
   - Посмотрите на оба результата
   - Понять влияние секции CRITICAL на точность переноса лица
   
4. **Test без STYLE**
   - Включите обратно CRITICAL
   - Выключите STYLE
   - Test → Result C
   - Понять влияние STYLE на освещение/композицию

### Сценарий 5: Отладка переменных

**Переменные** (например, `{{subject_mode}}`, `{{framing_hint}}`) подставляются в промпт автоматически.

1. **Изменить значение переменной**
   - Configuration → Subject Mode: `face` → `full_body`
   - JSON Preview покажет обновленный текст (все `{{subject_mode}}` заменены на `full_body`)
   
2. **Test**
   - Нажать "Test Prompt"
   - Результат теперь в полный рост, а не только лицо
   
3. **Сравнить варианты**
   - Test с `face` → portrait shot
   - Test с `full_body` → full body shot
   - Test с `upper_body` → waist-up shot

## API Endpoints (для разработчиков)

### `POST /admin/playground/test-prompt`

**Request:**
```
multipart/form-data:
- config_json: JSON string (PlaygroundPromptConfig)
- session_id: string (для SSE логов)
- image1: File (optional)
- image2: File (optional)
```

**Response:**
```json
{
  "success": true,
  "image_b64": "iVBORw0KGgoAAAA...",
  "raw_response": { "candidates": [...] },
  "duration_seconds": 8.45,
  "request_json": { "model": "...", "contents": [...] }
}
```

### `GET /admin/playground/logs/{session_id}`

**Response:** Server-Sent Events (SSE)
```
data: {"type": "connected", "session_id": "..."}

data: {"type": "log", "log": {"timestamp": "...", "level": "INFO", "message": "...", "extra": {...}}}

data: {"type": "heartbeat", "timestamp": "..."}
```

### `GET /admin/playground/load-trend/{trend_id}`

**Response:**
```json
{
  "trend_id": "...",
  "trend_name": "Victoria Secret",
  "config": {
    "sections": [
      { "id": "...", "type": "text", "label": "[CRITICAL]", "content": "...", "enabled": true, "order": 0 },
      { "id": "...", "type": "text", "label": "[SCENE]", "content": "...", "enabled": true, "order": 1 }
    ],
    "variables": { "subject_mode": "face", "framing_hint": "portrait" },
    "model": "gemini-2.0-flash-exp",
    "size": "1024x1024",
    "format": "png",
    "temperature": 0.7
  }
}
```

### `GET /admin/playground/default-config`

**Response:** Same as `load-trend` но с дефолтными секциями из `GenerationPromptSettings`.

## Технические детали

### Frontend Stack (2026)

- **React 18+** - UI framework
- **React Router v6** - routing
- **TanStack Query** - data fetching & caching
- **Zustand** - state management
- **Shadcn/ui** - UI components (Radix UI + Tailwind CSS)
- **Lucide React** - icons
- **Sonner** - toast notifications
- **SSE (Server-Sent Events)** - real-time logs

### Backend Stack

- **FastAPI** - Python async web framework
- **SQLAlchemy** - ORM для работы с БД
- **Pydantic v2** - data validation
- **asyncio** - async support
- **In-memory Queue** - для SSE логов (можно заменить на Redis Streams)

### Архитектура

```
┌─────────────────────────────────────────────┐
│         Frontend (React + Vite)             │
│  ┌─────────────────────────────────────┐   │
│  │  PromptPlaygroundPage.tsx           │   │
│  │  - Prompt Builder (sections)        │   │
│  │  - JSON Preview (request)           │   │
│  │  - Test Panel (images + button)     │   │
│  │  - LogViewer (SSE stream)           │   │
│  │  - ResultViewer (image + response)  │   │
│  └─────────────────────────────────────┘   │
│              ↓ API calls                    │
└─────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────┐
│      Backend (FastAPI + Python)             │
│  ┌─────────────────────────────────────┐   │
│  │  /admin/playground/                 │   │
│  │  - test-prompt (multipart)          │   │
│  │  - logs/{session_id} (SSE)          │   │
│  │  - load-trend/{trend_id}            │   │
│  │  - default-config                   │   │
│  └─────────────────────────────────────┘   │
│              ↓                              │
│  ┌─────────────────────────────────────┐   │
│  │  ImageProviderFactory               │   │
│  │  → GeminiProvider                   │   │
│  └─────────────────────────────────────┘   │
│              ↓ API call                     │
└─────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────┐
│          Gemini API 2.0                     │
│  generateContent(model, contents)           │
└─────────────────────────────────────────────┘
```

## Известные ограничения и TODO

### Текущие ограничения

- ❌ **Нет сохранения шаблонов** - нельзя сохранить промпт как шаблон (TODO: `/save-template`, `/load-template`)
- ❌ **Нет batch testing** - можно тестировать только 1 фото за раз (TODO: batch mode)
- ❌ **Нет автоматического apply to trend** - нужно копировать промпт вручную (TODO: "Deploy to Trend" button)
- ❌ **Нет side-by-side compare** - нельзя сравнить 2 результата рядом (TODO: compare view)

### Планы на будущее (Phase 2)

1. **Template Management**
   - Сохранение промптов как шаблоны
   - Загрузка шаблонов
   - Библиотека шаблонов (community shared)

2. **Batch Testing**
   - Загрузить 10+ фото
   - Прогнать один промпт через все фото
   - Grid view результатов

3. **Advanced Analytics**
   - Face similarity scoring (vision model)
   - Автоматическая оценка качества
   - Статистика по тестам (A/B testing)

4. **Integration**
   - "Deploy to Trend" - автоматическое применение промпта к тренду
   - "Load from Trend" - уже есть ✅
   - Sync с production трендами

## FAQ

**Q: Почему Gemini вернул IMAGE_OTHER?**
A: Проверьте `finish_message` в логах или Raw Response. Наиболее частые причины:
- Incompatible aspect ratios (используйте фото с одинаковым соотношением сторон)
- Face partially obscured (лицо закрыто рукой/волосами)
- Safety violation (контент фото/промпта нарушает safety policies)

**Q: Как понять, влияет ли секция на результат?**
A: Выключите секцию (👁️), test, сравните результат с baseline (все секции включены).

**Q: Можно ли изменить порядок изображений?**
A: Пока нет drag & drop для изображений, но вы можете загрузить их в другом порядке (поменять местами Image 1 и Image 2).

**Q: Как долго хранятся логи?**
A: Логи хранятся в памяти (in-memory queue) только во время активной SSE сессии. После отключения клиента очищаются.

**Q: Можно ли использовать Playground для других провайдеров (OpenAI, Replicate)?**
A: Пока Playground работает только с Gemini. Поддержка других провайдеров в TODO.

## Поддержка

Если возникли проблемы или есть идеи по улучшению:
- Создайте issue в репозитории
- Напишите в Telegram: @your_telegram
- Email: support@example.com

---

**Версия:** 1.0 (MVP)  
**Дата:** 2026-02-04  
**Автор:** AI Slop Team
